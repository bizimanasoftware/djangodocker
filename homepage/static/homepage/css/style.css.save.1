<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>DJ Management System</title>
  <style>
    :root {
      --main-bg-off-white: #fefefe;
      --section-border: #e0e0e0;
      --pink-accent: #ff6ec4;
      --blue-accent: #7873f5;
      --shadow-color: rgba(0, 0, 0, 0.1);
    }

    body {
      margin: 0;
      padding: 0;
      font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
      background-color: #fafafa;
      color: #333;
    }

    header {
      background: linear-gradient(135deg, var(--pink-accent), var(--blue-accent));
      color: white;
      padding: 1rem 2rem;
      text-align: center;
    }

    footer {
      background: linear-gradient(135deg, var(--pink-accent), var(--blue-accent));
      color: white;
      padding: 1rem 2rem;
      text-align: center;
    }

    main.container {
      background-color: var(--main-bg-off-white);
      margin: 100px auto 40px auto;
      padding: 1.5rem;
      padding-left: 5vw;
      padding-right: 5vw;
      max-width: 100%;
      width: 100%;
      border-radius: 8px;
      box-shadow: 0 0 15px var(--shadow-color);
    }

    h1, h2 {
      color: #333;
      margin-top: 0;
    }

    section {
      margin-bottom: 2rem;
      padding-bottom: 1rem;
      border-bottom: 1px solid var(--section-border);
    }

    .media-box {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
      gap: 1rem;
    }

    .media-item {
      background-color: #fff;
      border: 2px solid var(--section-border);
      padding: 1rem;
      text-align: center;
      border-radius: 8px;
      box-shadow: 0 0 5px rgba(0,0,0,0.05);
    }

    .media-item img,
    .media-item video,
    .media-item audio {
      max-width: 100%;
      border-radius: 4px;
    }

    /* Mobile responsive improvements */
    @media (max-width: 480px) {
      main.container {
        padding-left: 4vw;
        padding-right: 4vw;
        padding-top: 1rem;
        padding-bottom: 1rem;
        margin: 80px 0 30px 0;
        border-radius: 0;
        box-shadow: none;
      }

      header, footer {
        padding: 1rem;
        font-size: 1rem;
      }

      h1 {
        font-size: 1.5rem;
      }

      .media-box {
        grid-template-columns: 1fr 1fr;
      }
    }
  </style>
</head>
<body>

  <header>
    <h1>DJ Media Management</h1>
  </header>

  <main class="container">
    <section>
      <h2>Pictures</h2>
      <div class="media-box">
        <div class="media-item"><img src="media/sample1.jpg" alt="DJ 1" /></div>
        <div class="media-item"><img src="media/sample2.jpg" alt="DJ 2" /></div>
        <div class="media-item"><img src="media/sample3.jpg" alt="DJ 3" /></div>
      </div>
    </section>

    <section>
      <h2>Videos</h2>
      <div class="media-box">
        <div class="media-item">
          <video controls poster="media/thumb1.jpg">
            <source src="media/video1.mp4" type="video/mp4" />
            Your browser does not support the video tag.
          </video>
        </div>
        <div class="media-item">
          <video controls poster="media/thumb2.jpg">
            <source src="media/video2.mp4" type="video/mp4" />
          </video>
        </div>
      </div>
    </section>

    <section>
      <h2>Audios</h2>
      <div class="media-box">
        <div class="media-item">
          <audio controls>
            <source src="media/audio1.mp3" type="audio/mpeg" />
            Your browser does not support the audio element.
          </audio>
        </div>
        <div class="media-item">
          <audio controls>
            <source src="media/audio2.mp3" type="audio/mpeg" />
          </audio>
        </div>
      </div>
    </section>
  </main>

  <footer>
    &copy; 2025 DJ Management System. All rights reserved.
  </footer>

</body>
</html>
